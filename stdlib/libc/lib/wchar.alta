# TODO: check Windows for these definitions

export type wchar_t = int

##if platform == "linux"
	export type wint_t = unsigned int
##else if platform == "darwin"
	export type wint_t = int
##end if

declare literal export function wprintf(format: ptr const wchar_t, @CTranspiler.vararg data: any...): int
