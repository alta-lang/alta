import Size, int64 from "types"
export abs, rand from "libc/lib/stdlib"

export generic function power<T>(num: T, to: T): T {
  # anything to power of zero is 1
  if to == 0 return 1

  let result = num
  for i: Size in to::1 {
    result *= num
  }

  return result
}
